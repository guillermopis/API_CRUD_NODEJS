'use strict';
module.exports = (sequelize, DataTypes) => {
  var participantes = sequelize.define('participantes', {
    nombre: DataTypes.STRING
  }, {});
  participantes.associate = function(models) {
    // associations can be defined here
  };
  return participantes;
};